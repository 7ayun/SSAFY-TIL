# [Vue] 참고

---

## 1. ref 객체

### ref 객체가 필요한 이유

**일반적인 변수가 아닌 객체 데이터 타입으로 사용하는 이유는?**

- Vue는 템플릿에서 `ref`를 사용하고 나중에 `ref`의 값을 변경하면 자동으로 변경사항을 감지하고, 그에 따라 **DOM을 업데이트** 함 ("의존성 추적 기반의 반응형 시스템")
- Vue는 렌더링 중에 사용된 모든 `ref`를 추적하며, 이후 `ref`의 값이 변경되면 해당 `ref`를 사용하는 컴포넌트를 **다시 렌더링**
- 이를 위해서 **참조 자료형의 객체 타입**으로 구현한 것

```
일반 변수: 값이 바뀌어도 Vue가 감지 불가능 → DOM 업데이트 안 됨
ref 객체: 값이 바뀌면 Vue가 자동 감지 → DOM 자동 업데이트
```

- JavaScript에서는 일반 변수의 접근 또는 변형을 감지할 방법이 없기 때문
- 참고: https://vuejs.org/guide/essentials/reactivity-fundamentals.html#why-refs

### ref가 const인데 값이 변하는 이유

`ref()`는 내부적으로 **객체(Object)** 를 생성한다. `{ value: 초기값 }` 형태.

```javascript
const count = ref(0)
// 실제로는 → { value: 0 } 이라는 객체가 저장됨
```

- `const`는 **재선언(변수에 새 값 할당)** 을 막는 것
- `const`에 저장되는 레퍼런스 타입(배열, 객체)은 **주소값**이 저장됨
- 주소가 변하지 않는 이상, 내부 값이 추가/변경/삭제되어도 `const` 규칙에 위배되지 않음

```javascript
const arr = []
arr.push(1)  // ⭕ 가능! arr에는 배열의 주소가 저장되어 있기 때문
arr = []     // ❌ 불가능! 주소 자체를 바꾸는 것은 불가능

const count = ref(0)
count.value++  // ⭕ 가능! count 객체 내부의 value를 바꾸는 것
count = ref(1) // ❌ 불가능! count 변수 자체에 재할당하는 것
```

> "대부분 변수 선언할 때 기본적으로 const를 쓰는 이유가 재선언을 막기 위함이다. ref 타입도 주소가 바뀌는 것을 막기 위한 것도 있다."

### 반응형 변수 vs 일반 변수 비교

```html
<!-- ref_vs_variable.html -->
<div id="app">
  <p>반응형 변수: {{ reactiveValue }}</p>
  <p>일반 변수: {{ normalValue }}</p>
  <button v-on:click="updateValues">값 업데이트</button>
</div>
```

```javascript
const app = createApp({
  setup() {
    const reactiveValue = ref(0)
    let normalValue = 0
    const updateValues = function () {
      reactiveValue.value++
      normalValue++
    }
    return {
      reactiveValue,
      normalValue,
      updateValues
    }
  }
})
```

> **결과:** 버튼 클릭 시 반응형 변수만 화면에 업데이트됨. 일반 변수는 값이 바뀌어도 화면이 갱신되지 않음.

---

## 2. Ref Unwrap 주의사항

### 템플릿에서의 unwrap 시 주의사항

템플릿에서의 **unwrap은 ref가 setup에서 반환된 객체의 최상위 속성일 경우에만 적용**된다.

```javascript
// 이런 경우 문제 발생
const object = { id: ref(0) }
```

```html
{{ object.id + 1 }}  <!-- [object object]1 이 출력됨! -->
```

**[object object]1이 출력되는 이유:**

- `object`는 최상위 속성이지만 `object.id`는 그렇지 않음
- 표현식을 평가할 때 `object.id`가 unwrap 되지 않고 **ref 객체로 남아** 있기 때문
- 즉, unwrap 전에 표현식(`+ 1`)을 처리하므로, `object(ref 객체) + 1 = [object object]1`이 됨

**해결 방법:** id를 객체에서 **구조분해 할당으로 추출**하여 최상위 속성으로 만들기

```javascript
const object = { id: ref(0) }
const { id } = object  // 구조분해 할당으로 최상위 속성으로 추출
```

```html
{{ id + 1 }}  <!-- 1 출력 ⭕ -->
```

**단, ref가 `{{ }}`의 최종 평가 값인 경우는 unwrap 가능**

```javascript
const object = { id: ref(0) }
```

```html
{{ object.id }}           <!-- 0 출력 ⭕ -->
{{ object.id.value }}     <!-- 0 출력 ⭕ (동일한 결과) -->
{{ object.id + 1 }}       <!-- [object object]1 출력 ❌ -->
```

---

## 3. SEO (Search Engine Optimization)

**검색 엔진 최적화**

- google, bing과 같은 검색 엔진 등에 내 서비스나 제품 등이 효율적으로 검색 엔진에 노출되도록 개선하는 과정을 일컫는 작업
- 정보의 대상은 주로 HTML에 작성된 내용
- 최근에는 SPA, 즉 CSR로 구성된 서비스의 비중이 증가
- SPA 서비스도 검색 대상으로 넓히기 위해 JS를 지원하는 방식으로 발전하는 중

> SPA/CSR에서의 SEO 문제: 검색 엔진이 처음 페이지를 가져올 때 빈 HTML을 받기 때문에 콘텐츠를 확인하지 못해 검색 노출이 어려울 수 있음.

---

## 4. CSR과 SSR

### CSR & SSR

- CSR과 SSR은 흑과 백이 아님
- 애플리케이션의 목적, 규모, 성능 및 SEO 요구 사항에 따라 달라질 수 있음
  - 내 서비스에 적합한 렌더링 방식을 적절하게 활용할 수 있어야 함
- SPA 서비스에서도 SSR을 지원하는 Framework가 발전하고 있음
  - Vue의 **Nuxt.js**
  - React의 **Next.js**

| 방식 | 특징 | 적합한 경우 |
|------|------|-------------|
| CSR | 클라이언트에서 렌더링, 빠른 페이지 전환 | 사용자 인터랙션이 많은 대화형 앱 |
| SSR | 서버에서 렌더링, SEO 유리 | SEO가 중요하거나 초기 로딩이 중요한 경우 |

---

## 💡 한 줄 요약

> `ref`는 단순 변수가 아닌 객체 타입으로 구현되어 Vue의 의존성 추적이 가능하게 하며, 템플릿 언래핑은 최상위 속성일 때만 자동 적용된다는 점을 주의해야 한다.

## ❓ 더 찾아볼 것

- SEO 점수 측정 방법 및 최적화 가이드
- Nuxt.js (Vue SSR) 공식 문서
- Vue의 `reactive()` — ref 외 다른 반응형 API
- Proxy 객체 — Vue 3 반응형 시스템의 내부 구현 원리
