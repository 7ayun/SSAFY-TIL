# Day12 - 복습 노트

---

## 1) API

### 1-1. API 한 줄 정의
- API = 서로 다른 (__________)이/가 (__________)에 따라 (__________/__________)하는 방식

### 1-2. API는 누가 만든다?
- API를 만드는 주체: (__________)
- 사용자는 무엇을 보고 쓴다?: (__________)

### 1-3. API Key를 쓰는 이유 2가지
1) (__________)
2) (__________)

### 1-4. 메뉴판 비유로 설명하기
- 메뉴판 = (__________)
- 주문 = (__________)
- 요리 = (__________)

---

## 2) OAuth (소셜 로그인)

### 2-1. 가장 중요한 포인트 1줄
- 우리 서비스는 사용자의 (__________)를 직접 받지 않고 인증을 (__________)에 위임한다

### 2-2. 흐름 순서 채우기
1) 사용자가 “구글로 로그인” 클릭
2) 서비스가 (__________)로 이동
3) 사용자가 구글에서 로그인
4) 구글이 (__________)를 서비스에 전달
5) 서비스가 그 결과로 (__________) 처리

---

## 3) JSON

### 3-1. JSON의 본질
- JSON은 딕셔너리처럼 보이지만 실제로는 (__________) 이다

### 3-2. 왜 JSON을 쓰나? (핵심 1문장)
- 서로 다른 시스템은 어떤 언어를 쓰는지 모르므로 (__________) 형태로만 주고받는다

### 3-3. loads / dumps / load / dump 매칭
- json.loads(__________) : JSON 문자열 → (__________)
- json.dumps(__________) : dict → (__________)
- json.load(__________) : JSON 파일 → (__________)
- json.dump(__________, ________) : dict → JSON 파일 저장

### 3-4. s의 의미
- loads/dumps의 s는 (__________)를 의미한다

### 3-5. 함정 체크
- JSON 텍스트를 읽어온 변수의 타입이 str이면 `data['key']` 접근이 (가능/불가능)
- 파일 확장자가 .json이 아니어도 내용이 JSON이면 로드가 (가능/불가능)

---

## 4) MCP

### 4-1. MCP는 무엇인가?
- MCP는 도구가 아니라 (__________)이다  
- 정식 이름: (__________ __________ __________)

### 4-2. MCP가 필요한 이유 1줄
- 서비스마다 API 규격이 달라서 매번 연동 코드를 새로 짜는 (__________)이 발생한다

### 4-3. 구성요소 매칭(외울 필요 X, 의미만)
- MCP Server: (__________) 제공 주체
- MCP Host: 사용자 입력을 (__________)하고 어떤 도구를 쓸지 결정
- MCP Client: 실제 (__________) 및 통신 담당

### 4-4. 표준화 비유
- 라이트닝/HDMI 같은 제각각 → (__________)처럼 통일

---

## 5) VS Code MCP 실습

### 5-1. 설정 여는 단축 흐름
- `Ctrl + Shift + P` → (__________) 실행

### 5-2. 경로 설정 함정
- Windows 경로 `\`는 JSON에서 `\\`로 바꿔야 하는 이유: (__________)

### 5-3. 실습 프롬프트 3개 적기
1) 폴더 파일 목록: “________________________________”
2) 이미지 파일명 변경: “________________________________”
3) 사람/동물 분류 이동: “________________________________”

---

## 6) 실수 & 함정
- [ ] loads/dumps vs load/dump를 헷갈리지 않는다
- [ ] JSON은 “텍스트”라는 걸 잊지 않는다
- [ ] MCP는 “규약”이고, 채택/보안이 중요 포인트다
- [ ] 경로 백슬래시 이스케이프(\\)를 실수하지 않는다

---

### 이동
👉 [학습 정리](./README.md)
👉 [메인 README](../README.md)