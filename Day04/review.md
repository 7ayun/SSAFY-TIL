# Day04 - 복습 노트 (review.md)

> 빈칸에 들어갈 핵심 키워드/메서드/개념을 채우세요.

---

## A. 문자열 메서드

1. 문자열에서 특정 문자열의 위치를 찾되, 없으면 -1을 반환하는 메서드는 (        )

2. 문자열에서 특정 문자열의 위치를 찾되, 없으면 오류를 발생시키는 메서드는 (        )

3. 문자열에서 특정 부분을 다른 문자열로 바꾸는 메서드는 (        )

4. 문자열을 구분자 기준으로 나누어 리스트로 만드는 메서드는 (        )

5. 리스트의 문자열 요소들을 구분자로 연결해 하나의 문자열로 만드는 메서드는 (        )

6. 문자열 양쪽 공백을 제거하는 메서드는 (        )

7. 문자열의 대소문자를 서로 바꾸는 메서드는 (        )

---

## B. 리스트 메서드

8. 리스트 맨 뒤에 요소 하나를 추가하는 메서드는 (        )

9. iterable의 요소들을 펼쳐서 리스트에 추가하는 메서드는 (        )

10. 리스트에서 특정 위치에 요소를 삽입하는 메서드는 (        )

11. 리스트에서 요소를 삭제하고 반환하는 메서드는 (        )

12. 리스트에서 값 기준으로 요소를 삭제하는 메서드는 (        )

13. 리스트를 정렬하고 원본을 바꾸는 메서드는 (        )

14. 위 정렬 메서드는 반환값이 없어서 (        ) (이)다.

15. 원본을 바꾸지 않고 정렬된 새 리스트를 반환하는 파이썬 내장 함수는 (        )

---

## C. 얕은 복사 vs 깊은 복사

16. `a.copy()` 또는 `a[:]` 는 (        ) 복사이다.

17. 얕은 복사는 중첩 리스트처럼 내부에 가변 객체가 있을 때 내부 객체의 주소를 (        ) 한다.

18. 중첩 구조까지 완전히 복사하려면 `copy` 모듈의 (        ) 를 사용한다.

---

## D. 딕셔너리 메서드

19. 딕셔너리에서 키가 없을 때도 오류 없이 값을 가져오게 해주는 메서드는 (        )

20. `get('k', default)`에서 키가 없으면 반환되는 값은 (        ) 이다.

21. `dict.keys()` / `dict.values()` / `dict.items()` 는 복사본이 아니라 (        ) (이)다.

22. 딕셔너리를 순회할 때 (키, 값) 쌍을 가장 편하게 받는 메서드는 (        )

23. 딕셔너리에서 키를 삭제하면서 값을 반환하는 메서드는 (        )

24. 키가 없으면 (키, 기본값)을 넣고 그 값을 반환하는 메서드는 (        )

25. 다른 딕셔너리로 덮어쓰기/추가하는 메서드는 (        )

26. 딕셔너리의 모든 키/값을 삭제(초기화)하는 메서드는 (        )

---

## E. 세트 메서드

27. 세트에 원소 1개를 추가하는 메서드는 (        )

28. 세트에서 원소를 제거하되, 없으면 오류가 발생하는 메서드는 (        )

29. 세트에서 원소를 제거하되, 없으면 조용히 무시하는 메서드는 (        )

30. 세트는 순서가 없어서 pop()은 (        ) 원소를 제거하고 반환한다.

31. 세트에 다른 iterable의 요소들을 펼쳐서 추가하는 메서드는 (        )

32. 합집합 연산자는 (        )

33. 교집합 연산자는 (        )

34. 차집합 연산자는 (        )

---

## F. 메서드 체이닝

35. 메서드 체이닝은 여러 메서드를 (        ) 호출하는 방식이다.

36. 체이닝이 되려면 각 메서드가 다음 메서드를 호출할 수 있도록 (        ) 을 반환해야 한다.

37. 리스트의 `append()` 와 `sort()` 는 원본을 바꾸고 (        ) 을 반환하므로 체이닝이 깨진다.

---

## G. 해시 테이블 맛보기

38. dict/set이 빠른 이유는 내부적으로 (        ) 기반 구조를 사용하기 때문이다.

39. dict의 키는 보통 (        ) 타입이어야 한다. (예: int, str, tuple)

40. 키가 불변이어야 하는 이유는 키가 바뀌면 (        ) 값이 바뀌어 찾을 위치가 달라지기 때문이다.

---

### 🔗 이동

- 📘 학습 정리: [README.md](./README.md)
- ⬆ 메인: [README](../README.md)

