# Day02 복습 노트 (1월 23일)

---

# Part 1. Python 자료형 & 문법

## 1. 시퀀스 타입

* 시퀀스란: (                           )
* 공통 특징 4가지:

  1. (           )
  2. (           ) 가능
  3. (           ) 가능
  4. 길이 측정 → (           )

---

## 2. 문자열 (String)

* 변경 가능 여부: (           )

```python
s = "abc"
s[1] → (           )
s[0:2] → (           )
```

* 문자열 수정 시 발생: (           ) 오류

---

## 3. 리스트 (List)

* 변경 가능 여부: (           )
* 다양한 자료형 저장 가능 여부: (           )

```python
lst = [1, 'a', 3]
lst[0] = 100
결과 → (                      )
```

---

## 4. 리스트 인덱싱 O(1) 원리

```
주소 = (           ) + (           ) × 8
```

* 시간 복잡도: (           )

---

## 5. 튜플 (Tuple)

* 변경 가능 여부: (           )

```python
(1)   → 타입: (           )
(1,)  → 타입: (           )
```

---

## 6. range

```python
range(5)        → (                       )
range(1,5)      → (                       )
range(5,0,-1)   → (                       )
```

* 규칙: (           ) 포함, (           ) 미포함

---

# Part 2. Non-Sequence 타입

## 7. 딕셔너리 (Dictionary)

* 구조: (           ) : (           )
* 순서: (           )
* 변경 가능 여부: (           )

```python
d = {'apple':12}
d['apple'] → (           )
```

* Key 조건: (                               )

---

## 8. 세트 (Set)

* 순서: (           )
* 중복: (           )

```python
{1,2,3,3} → (                       )
```

* 주요 용도: (                           )

---

## 9. None / Boolean

* None 의미: (                           )

False 판정 값 3가지 이상 작성:

* (           )
* (           )
* (           )

---

## 10. 형 변환

```python
3 + 5.0 → (           )
int('10') → (           )
str(17) → (           )
```

---

## 11. 연산자

* 산술: +  -  *  /  (           )  (           )  **

* 비교 연산자 중

  * 값 비교: (           )
  * 주소 비교: (           )

---

# Part 3. Git 기초 & 로컬 버전 관리

## 12. Git 3대 영역

```
(           ) → (           ) → (           )
```

---

## 13. Git 기본 흐름

```
파일 수정 → (           ) → (           ) → (           )
```

---

## 14. Git 핵심 명령어

* 저장소 생성: (           )
* 상태 확인: (           )
* 스테이징 등록: (           )
* 커밋 생성: (           )

---

## 15. Git 색상 의미

* 빨간색: (                           )
* 초록색: (                           )

---

# Part 4. Git 원격 저장소 & 협업

## 16. 원격 저장소 연결

```bash
(           ) add origin <URL>
```

---

## 17. Push / Pull / Clone

* 업로드: (                           )
* 동기화: (                           )
* 전체 복제: (                           )

---

## 18. .gitignore

* 목적: (                           )

예시 2개 작성:

* (           )
* (           )

---

## 19. 실전 전체 흐름

```
(           )
→ 파일 작성
→ (           )
→ (           )
→ (           )
→ (           )
→ (           )
```

### 🔗 이동

- 📘 학습 정리: [README.md](./README.md)
- ⬆ 메인: [README](../README.md)